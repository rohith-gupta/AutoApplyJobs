# frontend

React application. Not yet implemented.

Will consume the REST API exposed by `services/job-platform-service` for
search/filter, save/pass, resume management, and application tracking.

See `../docs/architecture.md` and the root `CLAUDE.md` for the agreed
technology stack and constraints before adding code here.
