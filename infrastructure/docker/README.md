# infrastructure/docker

Local/deployment infrastructure. Not yet implemented.

Will hold Docker Compose configuration for local development (PostgreSQL,
`job-platform-service`, `job-ingestion-service`, and later Kafka), once
that step is reached. See `../../docs/roadmap.md` for sequencing — Docker
Compose is not part of the current milestone.
