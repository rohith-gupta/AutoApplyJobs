/**
 * Common module.
 *
 * <p>Shared building blocks with no single owning domain: cross-cutting
 * types, base classes, and utilities used by more than one module (e.g.
 * shared value objects, common exception types). Should stay small — most
 * logic belongs in its owning module, not here.
 */
package com.autoapplyjobs.platform.common;
