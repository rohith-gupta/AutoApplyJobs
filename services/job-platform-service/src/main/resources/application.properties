spring.application.name=job-platform-service

# No datasource is configured yet. DataSource/JPA/Flyway auto-configuration
# is excluded in JobPlatformServiceApplication until PostgreSQL is available
# (see the class Javadoc and CLAUDE.md's current milestone list) - this file
# intentionally carries no spring.datasource.* properties.
